import { Inter, Caveat } from 'next/font/google';

export const inter = Inter({
  weight: ['400', '500', '700'],
  subsets: ['latin'],
  display: 'swap',
});

export const caveat = Caveat({
  weight: ['700'],
  subsets: ['latin'],
  display: 'swap',
});
